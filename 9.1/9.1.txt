In order to have the expected result, the position for the inner class shpuld be changed to relative(instead of absolute).
That way the position is moved relativly to the width and height according to the webpage.
When selecting absolute position, there is a need to place it according to another "parent" (container class for example).